# Timar AI MCP Server

A Model Context Protocol (MCP) server and X402 payment protocol adapter for Timar payment, payout, and balance APIs.

## 🎯 What This Is

This project provides **two modes** of accessing Timar APIs:

| Mode | Transport | Use Case |
|------|-----------|----------|
| **MCP Server** | STDIO | Local AI assistants (Claude Desktop, Cursor) — API Key auth |
| **X402 Adapter** | HTTP | AI Agents with user-authorized wallets — X402 on-chain payment for transfers |

**It is:**
- An MCP server (stdio transport, no deployment needed)
- An X402 protocol adapter (HTTP server, pay-per-use API)
- A configuration and authentication adapter
- A capability router

**It is NOT:**
- A payment engine
- A business-rule host
- A replacement for Timar's public APIs

## 🌍 Supported Languages

The setup wizard supports 3 languages:

| Language | Code |
|----------|------|
| 简体中文 | `zh-CN` |
| 繁體中文 | `zh-TW` |
| English | `en` |

## 🛠️ Supported Tools

| Tool | Description | X402 Mode |
|------|-------------|-----------|
| `payment.create` | Create a new payment order | **Dynamic** — Agent pays actual transfer amount to Timar's receiveAddress |
| `payment.get` | Get payment order details | None — MCP API Key auth, no payment |
| `payment.cancel` | Cancel a payment order | None — MCP API Key auth, no payment |
| `payout.create` | Create a new payout order | **Dynamic** — Agent pays actual payout amount to withdrawAddress |
| `payout.get` | Get payout order details | None — MCP API Key auth, no payment |
| `balance.list` | List balances for all currencies | None — MCP API Key auth, no payment |

> **X402 only applies to transfer operations** (`payment.create`, `payout.create`). The X402 payment IS the actual business transfer — not a service fee. The Agent uses a user-authorized wallet to pay USDC directly to the address returned by Timar API. Query/cancel operations are free (protected by MCP-layer API Key).

## 📋 Prerequisites

- Node.js 24+ (for TypeScript native support)
- Timar merchant account with API credentials
- MCP-compatible AI assistant (Claude Desktop, Cursor, Cline, etc.)
- For X402 mode: a user-authorized wallet (Agent signs and broadcasts on-chain payments)

## 🚀 Quick Start (3 Steps)

### 1. Clone and Install

```bash
git clone https://github.com/your-org/TimarAIMCP.git
cd TimarAIMCP
npm install
```

### 2. Run Setup Wizard 🎯

```bash
npm run setup
```

The setup wizard will guide you through:

```
  Step 1: Choose language (简体中文 / 繁體中文 / English)
  Step 2: Choose environment (Sandbox / Production / Both)
  Step 3: Enter API domain, API Key, Secret Key
  Step 4: Configure X402 payment adapter (optional)
  Step 5: Auto-save configuration
  Step 6: Get AI client integration code
```

Example walkthrough (简体中文):

```
  ╔══════════════════════════════════════════╗
  ║       🚀 Timar AI MCP 配置向导          ║
  ╚══════════════════════════════════════════╝

  🌍 步骤 1/7：选择语言 / Select Language
    1. 简体中文
    2. 繁體中文
    3. English

  📡 步骤 2/7：选择要配置的环境
    1. Sandbox（沙箱测试环境）
    2. Production（生产环境）
    3. 两个都配置

  ❯ 🌐 Sandbox API 域名 [https://sandbox-api.timar.io]:
  ❯ 🔑 Sandbox API Key: ****
  ❯ 🔐 Sandbox Secret Key: ****

  💳 步骤 4/7：是否配置 X402 支付协议适配？
    1. 是，配置 X402
    2. 跳过，暂不需要

  ❯ 💰 收款钱包地址: 0xYourWalletAddress
  ❯ 🔗 Facilitator 服务地址 [https://facilitator.x402.org]:
  ❯ 🌐 HTTP 服务端口 [3402]:

  ✅ 配置完成！
```

### 3. Configure AI Client

Paste the configuration code generated by the wizard into your AI client. See [MCP Client Integration](#-mcp-client-integration).

---

## ⚙️ Configuration

### Interactive Setup Wizard (Recommended ✅)

```bash
npm run setup
```

Guides you step by step: choose language → choose environment → enter credentials → configure X402 (optional) → auto-save.

### Manual Configuration (Advanced)

```bash
cp config/runtime.config.example.json config/runtime.config.json
# Edit config/runtime.config.json with your credentials
```

#### Config File Format

```json
{
  "language": "zh-CN",
  "defaultEnvironment": "sandbox",
  "environments": {
    "sandbox": {
      "baseUrl": "https://sandbox-api.timar.io",
      "apiKey": "your-sandbox-api-key",
      "secretKey": "your-sandbox-secret-key",
      "timeoutMs": 5000
    }
  },
  "x402": {
    "payTo": "0xYourWalletAddress",
    "facilitatorUrl": "https://facilitator.x402.org",
    "port": 3402,
    "networks": ["base", "ethereum", "solana"]
  }
}
```

**Fields:**

| Field | Required | Description |
|-------|----------|-------------|
| `language` | Yes | Interface language: `zh-CN`, `zh-TW`, or `en` |
| `defaultEnvironment` | Yes | Default environment: `sandbox` or `production` |
| `environments.<env>.baseUrl` | Yes | API base URL |
| `environments.<env>.apiKey` | Yes | API key |
| `environments.<env>.secretKey` | Yes | Secret key |
| `environments.<env>.timeoutMs` | No | Request timeout in ms (default: 5000) |
| `x402.payTo` | X402 only | Fallback wallet address (optional — for receiveAddress extraction, Timar API provides the actual address) |
| `x402.facilitatorUrl` | No | Facilitator URL (default: `https://facilitator.x402.org`) |
| `x402.port` | No | HTTP server port (default: 3402) |
| `x402.networks` | No | Payment networks (default: `["base","ethereum","solana"]`) |

---

## 🔌 MCP Client Integration

> 💡 **Tip**: After running `npm run setup`, the wizard will automatically generate the configuration code — you don't need to write it manually.

### Claude Desktop

Config file location:
- macOS: `~/Library/Application Support/Claude/claude_desktop_config.json`
- Windows: `%APPDATA%\Claude\claude_desktop_config.json`

```json
{
  "mcpServers": {
    "timar-ai": {
      "command": "node",
      "args": ["/absolute/path/to/TimarAIMCP/scripts/start-stdio-server.mjs"],
      "env": {
        "TIMAR_MCP_CONFIG": "/absolute/path/to/TimarAIMCP/config/runtime.config.json"
      }
    }
  }
}
```

### Cursor

Config file location: `.cursor/mcp.json` in project root

```json
{
  "mcpServers": {
    "timar-ai": {
      "command": "node",
      "args": ["/absolute/path/to/TimarAIMCP/scripts/start-stdio-server.mjs"],
      "env": {
        "TIMAR_MCP_CONFIG": "/absolute/path/to/TimarAIMCP/config/runtime.config.json"
      }
    }
  }
}
```

---

## 💳 X402 Protocol Adapter

The X402 adapter exposes Timar APIs as HTTP endpoints. Transfer operations (`payment.create`, `payout.create`) require the Agent to complete an on-chain USDC payment — this IS the actual business transfer, not a service fee. Query/cancel operations are free (MCP API Key auth).

### Architecture

```
AI Agent (holds user-authorized wallet)
    ↓ POST /v1/payment/create (no X-PAYMENT header)
X402 Protocol Adapter
    ↓ Phase 1: Call Timar API → get receiveAddress + amount
    ↓ Return 402 { payTo: receiveAddress, amount: actual transfer amount }
AI Agent
    ↓ On-chain: pay USDC to receiveAddress
    ↓ POST /v1/payment/create (with X-PAYMENT proof)
X402 Protocol Adapter
    ↓ Phase 2: Verify on-chain payment via facilitator
    ↓ Confirm order → return result
```

### Starting the X402 Server

```bash
npm run start:x402
```

Or with a custom config path:

```bash
TIMAR_MCP_CONFIG=/path/to/config.json npm run start:x402
```

### X402 Endpoints

| Method | Path | MCP Tool | Payment |
|--------|------|----------|---------|
| `POST` | `/v1/payment/create` | `payment.create` | **X402 dynamic** (pay actual amount to receiveAddress) |
| `GET` | `/v1/payment/:orderId` | `payment.get` | None (MCP API Key) |
| `DELETE` | `/v1/payment/:orderId` | `payment.cancel` | None (MCP API Key) |
| `POST` | `/v1/payout/create` | `payout.create` | **X402 dynamic** (pay actual amount to withdrawAddress) |
| `GET` | `/v1/payout/:orderId` | `payout.get` | None (MCP API Key) |
| `GET` | `/v1/balance` | `balance.list` | None (MCP API Key) |

Free endpoints (no payment required):

| Method | Path | Description |
|--------|------|-------------|
| `GET` | `/health` | Health check |
| `GET` | `/v1/tools` | List available tools and pricing |

### Payment Flow Example (payment.create)

```
1. AI Agent sends:  POST /v1/payment/create { amount: 100, currency: "USDT", network: "base" }
2. Server calls:   Timar API → creates order → gets receiveAddress
3. Server returns:  402 { payTo: "0xTimarReceiveAddress", amount: "100", network: "base" }
4. AI Agent pays:   100 USDC to receiveAddress via user-authorized wallet
5. AI Agent sends:  POST /v1/payment/create + X-PAYMENT header (on-chain proof)
6. Server verifies: payment via facilitator
7. Server returns:  200 OK + order confirmed
```

### Query Flow Example (no payment needed)

```
1. AI Agent sends:  GET /v1/payment/:orderId
2. Server calls:   MCP router → Timar API (API Key auth)
3. Server returns:  200 OK + order details
```

### Supported Networks

| Network | Chain | Asset |
|---------|-------|-------|
| Base | L2 | USDC (`0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913`) |
| Ethereum | L1 | USDC (`0xA0b86991c6218b36c1d19D4a2e9Eb0cE3606eB48`) |
| Solana | SVM | USDC (`EPjFWdd5AufqSSqeM2qN1xzybapC8G4wEGGkZwyTDt1v`) |

---

## 🧪 Development

### Test

```bash
npm test
```

### Quick Test MCP Server

```bash
TIMAR_MCP_CONFIG=config/runtime.config.json npm start
```

### Quick Test X402 Server

```bash
TIMAR_MCP_CONFIG=config/runtime.config.json npm run start:x402
```

## 📝 Tool Input Examples

### payment.create

```json
{
  "environment": "sandbox",
  "merchantOrderId": "order-123",
  "merchantUserId": "user-456",
  "amount": 100.00,
  "currency": "USD",
  "network": "ethereum",
  "returnUrl": "https://your-app.com/return",
  "cancelUrl": "https://your-app.com/cancel"
}
```

### payout.create

```json
{
  "environment": "sandbox",
  "merchantOrderId": "payout-123",
  "merchantUserId": "user-456",
  "amount": 50.00,
  "currency": "USD",
  "network": "ethereum",
  "withdrawAddress": "0x..."
}
```

### balance.list

```json
{
  "environment": "sandbox"
}
```

## 🔒 Security

- **Never** commit your config file with real credentials
- **Never** share your `secretKey` with anyone
- The server signs requests internally — you don't need to worry about signing
- Use sandbox environment for testing
- X402 payments are verified by the facilitator before API calls execute

## 🐛 Troubleshooting

### "No configuration found" on first start

Run the setup wizard:
```bash
npm run setup
```

### Server won't start

- Verify your config file exists at `config/runtime.config.json`
- Check that all required fields are filled (baseUrl, apiKey, secretKey)
- Re-run `npm run setup` to regenerate

### X402 server fails to start

- Ensure `x402` section is set in your config
- Install dependencies: `npm install`
- Check port 3402 is not already in use

### Tools return errors

- Verify the environment (sandbox/production) matches your credentials
- Check that the API keys have the necessary permissions

## 📄 License

MIT

---

**Need help?** Open an issue on GitHub or contact our support team.
